package org.example.backend.enums;

public enum NotificationType {
    COURSE_APPROVED,
    COURSE_REJECTED,
    COURSE_PENDING,
    INFO
    // tamj thời chỉ notificate cho course status, mở rộng sau
}

