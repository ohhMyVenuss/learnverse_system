server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # Proxy các request API sang backend (tùy chọn)
    location /api/ {
        proxy_pass http://backend:8080;
    }
}